---
import { Icon } from 'astro-icon/components';

interface Props {
	icon: string;
}

const { icon } = Astro.props;
---

<div class="grid aspect-square h-8 w-8 shrink-0 place-items-center rounded-md bg-primary text-white">
	<Icon name={icon} width={18} height={18} />
</div>
